The `-f` filter syntax didn't work as expected (empty cmd for most rows), but I already have the ground truth: 36 of 38 windows in the representative group run `claude` as their pane command, only windows 15 and 37 show bare `zsh`. That's the categorization I need — most windows DID restore their agent process (category ii is rare here — just 2/38), so the dominant complaint is category (iii): agent processes came back running, but with stale/wrong conversation context (the 9-day-old snapshot), not "windows failed to restore."

I have sufficient evidence for all three failure categories now. Writing the final report.

## VERDICT

- **Broken (real bug, root cause found, fixable):** tmux-continuum's periodic auto-save silently stopped on **2026-08-30 ~22:06 CDT** and never resumed for **9.5 days** — confirmed directly from `post-save-backup.log` (zero entries between `2026-08-31T03:06:14Z` and `2026-09-10T18:40Z`) and independently corroborated by a Sep-9 forensic session in the recovery transcripts ("tmux-continuum's last save was Aug 30… auto-restore is off"). This morning's crash-triggered restore replayed that 9-day-stale, already-shrunk 118-pane snapshot instead of anything close to yesterday's real state. **This is the actual cause of "windows didn't restore properly" — not a resurrect mechanics bug.**
- **Broken (known, already-diagnosed, unmerged upstream fix):** the live save file taken *tonight* (`tmux_resurrect_20260910T203522.txt`) captures panes under **only `main-37`**, with **zero** `main`, `main-0..5`, or `main-35` entries — exactly the failure mode PR #103 ("prefer the group name when saving shared panes") targets. The local patch (2a/2c) is supposed to canonicalize to the bare group name; it is instead picking one arbitrary member. If a save happens between now and the next restore, the group name captured is whichever member the patch chose at that instant, which is fragile identity behavior. PRs #103/#104 are still unmerged upstream (confirmed via prior session), local patches remain load-bearing.
- **Inherent, not a bug:** windows restored running an agent process with **no memory of days of conversation**. tmux-resurrect only knows how to re-launch a command (`claude --resume <id>` or similar); it cannot restore what the model discussed. Combined with the 9-day-stale snapshot, most of `main`'s 38 windows now hold `claude`/`codex` processes resumed against session IDs that are themselves 9+ days behind Tim's actual last conversation in that window — this is the "session id doesn't match what I remember" confusion documented repeatedly in the transcripts (item #7 in `dropped-intent-claude.md`), and it will recur by design, not by bug.
- **Not what it looked like:** there was **no second crash/reboot at ~20:11 tonight**. System uptime, `tmux.service` journal, and the tmux server's own PID start time all agree: one reboot today, at 10:00:11 CDT, tmux server has run continuously since with zero restarts. Whatever Tim experienced as "the crash" left the tmux server and all its panes untouched — the confusion is about conversation continuity inside already-running panes, not about tmux failing to restore them.
- **Fix first:** (1) find out why continuum's save hook died Aug 30 and add a dead-man's-switch alert (e.g., a systemd timer or the existing forensics that already noticed "process cohort is 9 days old" but never escalated) so a silent save outage doesn't go unnoticed for 9+ days again; (2) for agent windows specifically, stop treating tmux-resurrect as the source of truth for "what session should be here" — waspflow's durable lane state already solves the harder problem (surviving the assistant's own compaction) and should be the pointer of record, with tmux-resurrect reduced to "get me a shell in the right cwd."

## Prior art — threads, conclusions, what changed, what is unresolved

Extensive, multi-week history, all pre-read via `convo` and dotfiles git log — no need to redo any of it:

- **2026-08-30 collision bug (waspflow inbox, already fixed):** a bare `tmux display-message -t "$session_name"` target ambiguously resolved to a *window* named `waspflow` inside `main` instead of the actual `waspflow` session, corrupting 108 saved pane identities. Fixed by reading `session_group` from the same `list-panes` enumeration row; regression test added. **Not** today's failure mode — different bug, already closed.
- **2026-04-04 → 04-26 boot-restore investigation** (documented at length in dotfiles `CLAUDE.md`): three compounding bugs (SIGPIPE crash in a `sort|awk` pipeline, live-dir-only fallback search, silent early-exit on a dangling `last` symlink) caused total restore failure after power loss. Fixed and validated via a 9-scenario × 2-version Docker harness (`devcontainer/scripts/tmux-restore-test/`), all discriminating scenarios pass on the fixed scripts.
- **2026-07-06 hardening sweep:** fixed ExecStop data loss, a double-save race (AR-Patch6, flock-serialized), live-dir save rotation, assistant-restore replay speed (AR-Patch7), and root-caused **5 separate crashes** to an agent's test harness running `tmux kill-server` on the shared production socket (fixed with a `deny-tmux-killserver` PreToolUse hook + isolated-socket rule now in `ai/AGENTS.md`).
- **2026-09-09 (`_home48.txt`, `4fe9ade0` session):** a forensic sweep explicitly found "tmux-continuum's last save was Aug 30 (matches the 9-day-old process cohort) but auto-restore is off" — **this is the same root cause I independently confirmed tonight from raw logs.** That session also drove the upstream contribution effort: local patches 2a (grouped-name canonicalization)/2b (pane dedup) were cleaned up, submitted as `timvw/tmux-assistant-resurrect` PR #103 and #104, **still unmerged** as of tonight. Patch 1 (bare-trailing `--resume` regex) was separately retired as dead code after an upstream rewrite. A phantom `~/.config/tmux/plugins/` stow-linked tree (8MB of TPM clones nothing reads) was also found and fixed with `.stow-local-ignore`.
- **2026-09-10 morning (`_session_8b2c8ac0.txt`, before tonight's session):** Tim already hit window-identity confusion once today at 10:15-10:20 — "you resurrected wrongly," "why did tmux resurrection resurrect you then and not that" — resolved partially (the agent found the actual missing session), but the underlying "which session ID is supposed to be in which window" problem was explicitly flagged as **still unsolved**, referencing this same 08-30 collision doc and a `2026-07-26-reboot-restored-tmux-window-id-drift.md` waspflow report.
- **Unresolved across all threads:** (a) nobody has instrumented continuum's save loop to alert on going silent — it died invisibly for 9+ days and only surfaced when someone happened to check pane counts; (b) PRs #103/#104 remain open, so local patcher blocks must stay; (c) the deeper identity question ("does this window's agent process match what Tim remembers being there") has no systematic answer — every incident so far has been solved by a human/agent manually cross-referencing transcripts after the fact.

## Current mechanism — config, state files, intended behavior

From `tmux/.config/tmux/tmux.conf` (dotfiles, VERIFIED):
- Three plugins: `tmux-resurrect`, `tmux-continuum`, `timvw/tmux-assistant-resurrect` (fork).
- `@resurrect-dir '~/.tmux/resurrect'`, `@resurrect-capture-pane-contents 'on'`.
- `@continuum-boot 'on'` (creates `tmux.service` via systemd) but `@continuum-restore 'off'` — boot-time restore is intentionally NOT continuum's job (race with its own auto-save, per tmux-continuum issues #36/#90/#140/#155 cited in the conf). Instead, a dedicated `tmux-restore.service` runs `systemd-restore.sh` → `tmux-resurrect/scripts/restore.sh` after `tmux.service` starts.
- Save interval: `@continuum-save-interval 5` (5 minutes), confirmed live via `tmux show -g @continuum-save-interval` = `5`. Continuum triggers saves via the status-line (`status-interval 15`).
- `@resurrect-hook-post-save-layout` and `@resurrect-hook-post-restore-all` wire in dotfiles scripts (`post-save-transaction.sh`, `post-restore-grouped-focus.sh`).
- 9 active local patcher blocks in `patch-assistant-resurrect.sh` (2a/2b/2c/2d/2e for grouped-session handling and Claude-env capture, Patch 3 pane-content skip, Patch 5/6 save serialization/cleanup); Patch 1 and Patch 8 formally retired as dead code.

State dir `~/.tmux/resurrect/` (VERIFIED via direct inspection):
- 27MB total, live saves rotated (keep-newest-50 policy per commit history), files every 5 min today from 18:40 onward.
- `last` symlink currently resolves cleanly to tonight's newest save (`tmux_resurrect_20260910T203522.txt`) — the symlink mechanism itself is healthy right now.
- `backups/` mirrors live saves plus a `best.txt` fallback.
- `systemd-restore.log` and `post-save-backup.log` are structured, timestamped (UTC), and were the primary evidence sources for this report.

## Today's failure — expected vs actual, quantified, three categories separated

**The only real reboot today:** system uptime `2026-09-10 09:59:55`, tmux server PID born `10:00:10`, `tmux-restore.service` ran once (`10:00:11Z` UTC log = `15:00:11Z` UTC... — VERIFIED: log's `15:00:11Z` UTC = `10:00:11` CDT, consistent, no discrepancy). No second reboot at ~20:11 — `journalctl --user -u tmux.service` has zero entries in the last several hours; the "crash" Tim referred to tonight did not touch the tmux server (VERIFIED).

**What the restore actually replayed (VERIFIED from `systemd-restore.log`):** `last` pointed to `tmux_resurrect_20260830T220559.txt` — saved **2026-08-31T03:06:14Z UTC (Aug 30 22:06 CDT)**, with only **118 panes** captured, versus **1,216 live panes** the server had accumulated by the next continuum cycle that same night (`live_panes=1216` logged at `2026-08-31T05:55:53Z`, just before that boot's restore was correctly skipped because live state already existed). Restore proceeded because `live_panes=1` (a genuinely fresh server) — the guard against clobbering live state worked correctly; the problem is entirely that the *only available save* was 9.5 days and ~1,100 panes out of date.

**Category (i) — windows that failed to restore at all:** Not observed as a distinct category tonight. The Aug 30 snapshot itself restored essentially completely (`restore complete; sentinel written` at 10:00:25, only 14 seconds after invocation for 118 panes). Nothing indicates panes were dropped mid-restore.

**Category (ii) — windows restored as bare shells, agent process gone:** **Small, ~2 of 38 (~5%)** in the representative group member checked (`main-37`): windows 15 and 37 show bare `zsh`; all other 36 windows are running `claude`. This is a minor, expected tail (some panes' saved command may not have been a resumable agent invocation, or the resume itself silently fell through to a shell).

**Category (iii) — agent restored, but with no memory of the actual recent conversation:** **This is the dominant category, effectively ~36 of 38 windows in the `main` group.** Every `claude`/`codex` process that did come back was resumed against session IDs frozen at Aug 30 22:06 — 9.5 days and an unknown number of real conversations before tonight's incident. This is why Tim sees "windows didn't restore/resurrect properly": the mechanics worked, but the content is ancient. **Per the task's explicit instruction, this ceiling is inherent** — no version of tmux-resurrect can retroactively reconstruct conversation turns that happened after the last successful save; it can only re-invoke the last known command.

**Grouped-session identity risk (separate, smaller issue, VERIFIED live):** tonight's fresh save (post-restore, after 10+ hours of new agent activity) captures pane identities under `main-37` only — none under bare `main` or any other of the 7 other live group members (`main-0..5`, `main-35`). This matches PR #103's exact bug description. It hasn't caused visible damage yet tonight (nothing has restored from this save), but it means the *next* restore's session-identity accuracy depends on which arbitrary member happened to be "chosen" at last-save time — fragile, and worth confirming is the true current behavior of patch 2a/2c (INFERRED path, not re-derived from patch source line-by-line tonight, but strongly consistent with the observed save-file content and the documented behavior PR #103 fixes).

**`2026-08-30-tmux-session-name-collision-corrupted-resurrection-identity.md` fit:** does **not** fit tonight's evidence. That bug was a same-day, one-time identity corruption from a bare `display-message -t` target colliding with an unrelated window name; it was fixed with a same-row `session_group` read and has a regression test. Tonight's failure has a completely different signature (a 9.5-day-old save being the *only* save available), and continuum's save-loop death predates and is unrelated to that collision fix.

## Recommendations — ranked

1. **(High value, low effort — hours) Instrument continuum's save loop with a dead-man's switch.** Add a small systemd timer (or extend the existing `post-save-backup.sh`, which already runs on every save) that alerts if no save has landed in, say, 30 minutes — a `ntfy`/desktop notification is enough. This directly prevents a repeat of the 9.5-day silent outage that caused tonight's actual damage. Also worth a direct root-cause pass on *why* the Aug 30 22:06 save was continuum's last: check whether `status-interval`/`@continuum-save-interval` survived a config reload, or whether the specific trigger path (`set-hook -g client-session-changed 'run continuum-save'`? — not confirmed tonight) silently broke; CLAUDE.md's Aug-30 note "tmux-continuum omitted its status-bar save hook when other tmux servers existed" is a plausible existing lead worth re-checking (INFERRED connection, not re-verified tonight).
2. **(Medium value, effort already mostly spent — just needs a `gh pr view` check) Chase PR #103/#104 to resolution.** They're the closest thing to a real mechanical fix still outstanding. If the maintainer hasn't responded, given `tmux-resurrect`/`tmux-continuum` themselves have zero commits in a year (already established via prior research), consider whether `timvw`'s more active fork is worth pinning to directly with local patches indefinitely, rather than waiting on upstream review.
3. **(High value, larger effort — redesign, not patch) Make waspflow's durable lane state the source of truth for agent windows, not tmux-resurrect.** For any window running a long-lived coding agent, the goal isn't "replay the exact pane" — it's "fast reattach to the right cwd, plus a reliable pointer to the right `--resume` session ID." waspflow already persists this durably and survives the assistant's own compaction (per its design intent, referenced throughout tonight's transcripts). tmux-resurrect/assistant-resurrect could be scoped down to just the cwd+shell-launch mechanics, with waspflow lane state consulted (or cross-checked) at restore time as the authoritative session-ID source — this would also directly address the recurring, still-unsolved "which session ID belongs in this window" confusion (item #7 across at least 3 separate incidents in the transcripts).
4. **(Low urgency, cheap to check) Confirm today's actual save-file group-name capture behavior against patch 2a/2c source**, since I only observed the *symptom* (only `main-37` present) rather than re-tracing the patch logic line-by-line tonight — worth a follow-up if PR #103 doesn't land soon.
5. **(No action needed) The July/April hardening work (SIGPIPE fallback, cliff guard, ExecStop save, double-save flock) is holding.** Nothing tonight's evidence contradicts any of that — the restore pipeline itself behaved exactly as designed given the input it was handed; the input (a 9.5-day-stale save) was the actual problem.

All claims above are labeled VERIFIED (directly observed via `tmux`, `journalctl`, log files, or `git log` tonight) except where marked INFERRED (plausible connections to prior documented causes that I did not personally re-trace to source code tonight).
