# This account deliberately requires both an authorized key and a PAM-verified
# account password. PAM receives the password so pam_kwallet5 can unlock the
# login-password-encrypted wallet in a headless session.
UsePAM yes

Match User tnunamak
    AuthenticationMethods publickey,password
    KbdInteractiveAuthentication no
    PasswordAuthentication yes
