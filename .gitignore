# Claude Code runtime (repo root only, not the stow package)
/.claude/
.nvimlog

# Playwright MCP logs
.playwright-mcp/

# Session artifacts
SESSION-NOTES.md

# tmux plugins (installed by tpm at runtime)
tmux/.config/tmux/plugins/
