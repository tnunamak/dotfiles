# Override Kubuntu's system autostart entry by desktop-file ID.  The systemd
# override keeps its daemon pkcs11-only; this closes the separate desktop
# autostart path that would otherwise launch GNOME Keyring's secrets component.
[Desktop Entry]
Hidden=true
