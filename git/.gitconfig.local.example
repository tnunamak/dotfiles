# ~/.gitconfig.local — machine-specific git config (not tracked)
# Copy to ~/.gitconfig.local and fill in.

[user]
	name = Tim Nunamaker
	email = tnunamak@gmail.com
	signingkey = ~/.ssh/id_ed25519.pub
