{
  "schema": 1,
  "name": "clawmeter-codex-smoke",
  "baseline": "baseline_no_user_config",
  "model": "gpt-5.5",
  "reasoning": "low",
  "sandbox": "workspace-write",
  "env": {
    "GOCACHE": "{repo_dir}/.gocache"
  },
  "replicates": 1,
  "timeout_seconds": 1800,
  "tasks": [
    {
      "id": "clawmeter-openai-path-recovery",
      "repo": "/home/tnunamak/code/clawmeter",
      "base_ref": "9a2abf7ff463ee9b970fbd02ec74621e84aee4b7",
      "prompt": "Fix OpenAI/Codex detection for tray-launched Clawmeter when the user has Codex installed in a shell PATH that is not visible to the GUI process. Keep the change focused, preserve existing behavior for configured providers, and add/update tests for the path recovery behavior.",
      "success_commands": [
        "go test ./..."
      ],
      "success_timeout_seconds": 300
    }
  ],
  "variants": [
    {
      "id": "baseline_no_user_config",
      "label": "Baseline: no user config, MCP, hooks, or repo rules",
      "codex_args": [
        "--ignore-user-config",
        "--ignore-rules"
      ]
    },
    {
      "id": "current_stack",
      "label": "Current Codex config: context-mode hooks/MCP plus Headroom MCP"
    },
    {
      "id": "semmap",
      "label": "SEMMAP map included before editing",
      "enabled": false,
      "disabled_reason": "Set enabled=true and SEMMAP_BIN after installing semmap and its paired omni-ast checkout.",
      "requires": {
        "env": [
          "SEMMAP_BIN"
        ]
      },
      "setup_commands": [
        "\"$SEMMAP_BIN\" generate --root \"{repo_dir}\" --output \"{run_dir}/SEMMAP.md\""
      ],
      "prompt_prefix": "Before searching broadly, inspect the generated SEMMAP at {run_dir}/SEMMAP.md and use it to choose the smallest set of files to read."
    },
    {
      "id": "repomix_pack",
      "label": "Repomix pack included before editing",
      "enabled": false,
      "disabled_reason": "Disabled by default because whole-repo packs can spend significant tokens; enable only for a deliberate packer comparison.",
      "requires": {
        "commands": [
          "npx"
        ]
      },
      "setup_commands": [
        "npx --yes repomix@latest \"{repo_dir}\" --output \"{run_dir}/repomix.md\""
      ],
      "prompt_prefix": "A Repomix pack is available at {run_dir}/repomix.md. Use it only if it helps avoid broader repo discovery."
    }
  ]
}
