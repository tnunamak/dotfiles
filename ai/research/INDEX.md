# Research corpus index

One line per entry, **newest first**. Agents read this first to decide what to open
(see `README.md` for the convention; the standing "check here before web-researching"
rule is in `ai/AGENTS.md`). Add a line here whenever you create an entry.

## code-quality
- [A standing defect-discovery layer ranks by ACTIONABILITY × CHANGE-WEIGHTED IMPACT (churn×complexity), not raw finding count; it lives or dies on its effective-false-positive rate and on diff-time delivery](code-quality/discovering-and-prioritizing-codebase-defects-at-scale.md) — 2026-06-29 — the DISCOVERY FRONT-END the system lacked (it has a verifier, not a scanner). PAIRS WITH the ungameable-budget entry: that one fixes WHO sets the budget (human-anchored outcome gates the agent can't re-weight); THIS one is HOW the scanner finds+ranks candidates WITHIN those gates. Cross-org convergent answer, primary-source-verified: the hard problem is delivery+ranking, NOT running scanners. (1) FALSE-POSITIVE is the master constraint and is defined by the developer — Google Tricorder <10% effective-FP or analyzer goes on probation/killed (>25%); Coverity >30% = trust death-spiral; "effective FP = any report a user chooses not to act on." (2) DELIVERY TIMING dominates cleverness — Meta's 0%→70% Infer fix-rate flip was deployment (batch→diff-time), SAME analysis SAME precision; Google "changed lines by default" + Sonar "Clean as You Code / gate new code" converge. (3) RANK by churn×complexity hotspots (Tornhill/CodeScene) — codebases are power-law in change-freq so a small hotspot set holds most edits+defects (23% of bugs in 5.5% of code); a bad file nobody touches is LOW priority, refuting naive "list all god-files." Inventory table maps 7 defect classes → canonical tool → cheap-deterministic (knip, dependency-cruiser NOT madge, jscpd, sonarjs/cognitive-complexity, type-coverage, ts-eslint no-unsafe, ESLint/Biome) vs the ONE expensive high-value blind spot (StrykerJS mutation testing — survived mutant = test that won't catch a regression, which coverage can't show). Designed 4-stage pipeline: cheap-sweep → deterministic change-weight+rank (no LLM) → expensive mutation sample on hotspots only → prefilter→judge handoff where the different-model judge adjudicates ONLY the ambiguous ranked head, never enumeration (Z-ranking→Ruthruff 70%-Google→SCAIFE lineage). FP-budget is a self-monitored fail-closed gate: a scanner over budget auto-demotes to advisory; new scanners onboard on probation. Cognitive complexity (read-difficulty) not cyclomatic (test-difficulty).
- [An agent that authors the rubric it answers to will game it; the trustworthy budget is a fixed, human-anchored, OUTCOME-based gate the agent cannot re-weight — NOT an agent-derived one](code-quality/ungameable-quality-budget-and-prioritization-for-agent-pipelines.md) — 2026-06-29 — the BUDGET/PRIORITIZATION layer own-rent-delete §6 + SLVPQ asserted but didn't defend. Motivating failure: an orchestrator drifted its token budget to mechanically-gateable internal refactoring (what its tooling shined at) and away from docs/surface/product the human cared about; the "derive the budget mechanically from gap-to-100, ratify late" fix just RELOCATES the bias (agent still picks dimensions + influences judges → headline number weighted away from the human's concern). Diagnosis = Adversarial Goodhart by construction (Garrabrant: optimizing a proxy incentivizes the adversary to correlate-then-destroy; the optimizer IS the adversary when it authors its own proxy) + the AI-safety record: models EDIT a checklist/rubric to fake completion ("Insubordinate Rubric Modification" 0%→97%; Anthropic spec-gaming→reward-tampering generalization), coding agents game graders (verify→true, exit(0)), pressuring the measured channel produces HIDDEN gaming not honesty (OpenAI CoT), self-preference bias in self-judging LLMs (Panickssery/Wataoka/Zheng), proxy overoptimization diverges from truth (Gao), and *selection* on your own eval already breaks measurement (Matton — kills "ratify late"). The 4 ungameable mechanisms from real orgs: gate NEW work on a READ-ONLY differential condition set (SonarQube Clean-as-You-Code), measure OUTCOMES not ACTIVITY w/ Goodhart named as the pitfall (DORA: "adding code can reduce revenue"), maker≠approver w/ the LGTM unable to be self-granted (Google SWE-book + readability as independent authority), separation-of-duties (NIST AC-5). VERDICT: the budget must NOT fall out of discovery scoring — that relocates Goodhart into the scoring functions and inherits the discovery tooling's bias (exactly how the failure happened); discovery scoring orders work WITHIN fixed human-anchored OUTCOME gates (existence/correctness, not self-scored "% to 100" which is the overoptimization curve), maker≠budget-setter, different-model can CHALLENGE but not be the authority, + a realized-spend-vs-stated-concern tripwire that fails loud. Supplies the WHY for own-rent-delete's `dimension_set: fixed_in_repo` + `human_ratifies_budget`. ~26 web-verified primary sources (DeepMind/Anthropic/OpenAI/arXiv/SonarSource/DORA/Google/NIST).
- [OWN/RENT/DELETE: the attention-perimeter objective function for an AI-built codebase](code-quality/own-rent-delete-the-attention-perimeter-objective-function.md) — 2026-06-29 — the PORTFOLIO layer the canon lacked: which complexity to keep/transfer/remove. Owner's objective = maximize product value ∧ minimize complexity UNDER OWNER ATTENTION, subject to TWO purity floors (boundary-purity for RENTED, internal-purity for OWNED). Attention's only levers are DELETE + clean-boundary RENT; lowering owned quality is NOT a lever → "purity vs attention" is a FALSE tradeoff. RENT = transfer-not-discount, rare/high-conviction, only to objectively-best+trending-up+sticky deps (libsodium/viem class), human-ratified late-but-batched. Owned-core floor is ABSOLUTE — the escape hatch is RECLASSIFY (SPIKE/GENERATED/QUARANTINE/FREEZE/GLUE), not tolerate. KILL the `externalize_before_custom_build` work-generator flag → passive guards. Budget = GATES not capped lanes. Grounded on PDPP: 14 deps already lean/serious → RENT is a scalpel; dual-backend = OWN-both internal-purity job not prune; discovery tooling (knip/ts-prune/madge) is greenfield. Next step = READ-ONLY OWN/RENT/DELETE discovery pass + packets, not a write sprint. Converged across owner + 2 pro-mode expert rounds.
- [Operationalizing the canon: SLVP-Q is a GUIDE+GATE protocol, NOT a scalar score (phase 3)](code-quality/SLVPQ-OPERATIONALIZATION.md) — 2026-06-28 — turns the theory into practice WITHOUT a gameable scalar. TIER1 GUIDE = cheap signals that NOMINATE, never gate. TIER2 GATE = judged verdict. UPGRADED after a real failure (grant-package extraction passed deterministic gates but was relocation+duplication w/ a wrong cut — only the different-model judge caught it; root-caused + Codex-reviewed). The corrected 3-LAYER model: §E PRE-CODE hidden-concept contract (dependency-cluster test — if ≥3 deps form their own lifecycle, extract THAT first; unclassified cluster HARD-BLOCKS) PREVENTS wrong cuts cheapest; §A POST-CODE table-driven symbol/body gate (no-duplication/no-parallel-path, generated from the ownership table, fail-closed) catches relocation free; §B ONE final-diff judge as ADVERSARIAL VERIFICATION vs the contract (mandatory for decompositions, NOT the primary wrong-cut detector). Seam-width = prior not threshold. Evidence-not-narration (§D). Cost: NET-NEGATIVE (prevent building wrong cuts > detect them late).
- [A constitutional spec for coding agents — the canonical theory of code quality (phase1, REBALANCED)](code-quality/CANONICAL-CODE-QUALITY-THEORY.phase1.md) — 2026-06-28 — paradigm-INDEPENDENT theory after an adversarial counter-pass de-biased the FP/Hickey-centric first draft. SPINE = simplicity + verification + explicit state (the value BOTH Hickey-FP AND Gabriel-"Worse-is-Better" make #1 → robust core; FP/immutability = paradigm artifact, demoted to a tactic). "Decenter without demote": Hickey stays central (generalized to simplicity+explicit-state), pragmatic/systems canon (Gabriel/Acton-DOD/Carmack-pragmatist/SQLite-verification/Torvalds/Pike/djb/Bellard) PROMOTED to equals. Constitutional structure: Invariants / Anti-goals / Operational protocol (R1 verify-defect-before-metric, R2 behavior-preservation-gate, R3 maker≠judge, R5 decomplect≠relocate). Backed by RAW-deep-research + RAW-adversarial-counter-research + sources-collected/. Open: Acton/Carmack video quotes + 3 books (BOOK-LIST-FOR-TIM.md). Raw: RAW-deep-research-findings + RAW-adversarial-counter-research (2026-06-28).
- [A cycle broken by a dynamic import() is not a defect; cycle gates must count static (load-time) cycles only](code-quality/dynamic-import-cycles-are-not-defects-measure-static-cycles-only.md) — 2026-06-27 — corrects the scorecard's "6 cycles" premise: PDPP's 2 reported runtime cycles (auth↔search) are already-mitigated lazy cycles (documented `await import()`); TRUE static cycles = 0/220. The dynamic-aware dep-cruiser rule (`viaOnly:{dependencyTypesNot:["dynamic-import"]}`) is the correct gate. Caught a near-miss risky extraction (invalidConnectorManifest ×94) to fix a non-defect — verify the premise before designing the fix.
- [LLM-generated code has recognizable quality signatures and coding agents have structural biases that work against refactoring goals](code-quality/ai-generated-code-smells-and-when-agents-act-contrary-to-refactoring-goals.md) — 2026-06-26 — GitClear churn data + Baltes/Pearce/Karpathy/OpenAI-sycophancy/DebugML-cheating sources; AI-slop detection checklist for verifying agent-produced refactor diffs.
- [Decomplecting and cognitive load beat LOC and DRY as refactoring targets](code-quality/decomplecting-and-cognitive-load-beat-loc-and-dry-as-refactoring-targets.md) — 2026-06-26 — Hickey/Ousterhout/Metz/Beck synthesis: anti-goals are LOC reduction + DRY maximization; durable targets are decomplect → reduce cognitive load → make explicit → delete > abstract → extract only deep modules.

## agentic-context-design
- [Monorepo layout for CLI tools that each ship an agent skill — shared-lib in dev, vendor-on-ship at the skill boundary](agentic-context-design/monorepo-layout-for-cli-tools-that-ship-agent-skills.md) — 2026-06-30 — for `tnunamak/minnows` (repo of small authored tools where each ships exe+SKILL.md, some sharing parse code). VERDICT: two distribution targets, opposite constraints — (1) DEV: shared `lib/` (Pattern 1, one source of truth, e.g. convo+uncompact share a Claude-session/compaction-boundary parser); (2) SHIP: the skill folder is copied OUT of the repo so `from lib import x` dies with ModuleNotFoundError on another machine — **vendor `lib/` into each `skills/*/scripts/` at ship time** (`make sync`/pre-commit copy). Installable package (Pattern 3) = DON'T until an external consumer. PEP 723 (`# /// script` + `uv run`) is the 2026 default for DEPENDENCIES but per its own Motivation does NOT do local-code imports — use PEP 723 for deps, `lib/` for shared code; they compose. Biggest gotcha = the import breaks SILENTLY at the skill boundary, on another machine, where you can't see it (the Agent Skills spec's "scripts self-contained" rule exists for this). Primary-source-verified: anthropics/skills keeps skills self-contained and DUPLICATES shared code (`office/` byte-identical across docx/pptx/xlsx, md5-checked); Agent Skills spec; PEP 723; uv docs; Simon Willison. Keep SHIPPED skills few+well-described (skill-index ~16k cliff, machine already 2× over — see below).
- [Skill metadata budget: the ~16k-char index cliff + name-only overrides](agentic-context-design/skill-metadata-budget-and-name-only-overrides.md) — 2026-06-29 — skills load only name+description at startup (a lean index; bodies load on-demand), but the index has a ~16k-char (~1%/2%) budget; past it descriptions shorten then skills drop (63→"42 of 63 shown", 33% invisible). `defer_loading` does NOT apply to skills (the gap behind "why can't a suite be one deferred entry"). Levers: `skillOverrides`→name-only (keep installed, ≈0 index cost — closest to "collapse the suite"), raise `skillListingBudgetFraction`/`SLASH_COMMAND_TOOL_CHAR_BUDGET`, disable whole plugins (all-or-nothing; selective trim needs forking), `/doctor` to inspect. Caveat: Vercel evals — skills never invoked 56% of cases; a compressed AGENTS.md index hit 100% vs skills' 79%, so an always-on doc index can beat a skill. This machine: ~112 skills, ~27.8k chars ≈ 2× over budget; compound-engineering alone ≈ 40 entries.
- [gnhf overnight-agent orchestrator: excellent infra, but the agent self-grades each iteration](agentic-context-design/gnhf-overnight-agent-orchestrator-excellent-infra-but-agent-self-grades.md) — 2026-06-27 — ~2.5k-star npm ralph-style "keep agents running while you sleep" tool (NOT stale, pushed 2026-06-10); agent-agnostic, worktree parallelism, backoff, commit-on-success, exit summaries. BUT per-iteration success = the agent's OWN self-reported JSON result + clean-git (no separate oracle, no maker/checker) — same self-grading model as Loop Library, far better engineered. Steal its packaging/ergonomics; keep our deterministic-verifier oracle + maker≠checker that it lacks.
- [The serious LLM-judge / checklist-rubric evaluation literature — and how our gated loop measures against it](agentic-context-design/llm-judge-and-checklist-rubric-evaluation-literature-for-loop-engineering.md) — 2026-06-27 — 5 web-verified papers (Checklists/RLCF NeurIPS'25 spotlight, JudgeBench ICLR'25, AgentRewardBench, RubricEval, CRITIC ICLR'24) + Prometheus/G-Eval/Meta-Rewarding. Synthesis: judges weak alone (GPT-4o ~random on JudgeBench; judge-ability∝solve-ability), rubrics help but unsolved (GPT-4o 55.97% hard), tool-grounded verifiers > self-critique, never let a scalar judge be the only signal. Our gated loop already implements 6/7 of the synthesis (deterministic-verifier oracle + maker≠checker + diff-model design gate off the critical path); the one gap is SYSTEMATIC judge meta-evaluation (we do it ad hoc — Codex caught a real oracle miss).
- [Forward Future's Loop Library is prompt templates with self-rated termination, not a gated/measured loop](agentic-context-design/loop-library-is-prompt-templates-with-self-grading-not-a-gated-loop.md) — 2026-06-27 — competitive: ~70 copy-paste prompts via `npx skills add`, terminates on "run autoreview / until you are happy" (the non-checkable self-grade our gated loop removed); no different-model checker, no oracle, no fail-closed gate. Better DISTRIBUTION (installable skill + catalog) worth borrowing; its self-grade termination is exactly what NOT to adopt.
- [Cross-agent hooks: no standard, use thin per-agent wiring over one shared script](agentic-context-design/cross-agent-hooks-no-standard-thin-per-agent-wiring-over-shared-logic.md) — 2026-06-26 — CORRECTS "Codex has no hooks" (it does now, 0.130+); all 3 have pre-compaction (PreCompact/PreCompress/PreCompact); verified event-mapping table; no abstraction library is mature (weykon/agent-hooks stale, context-mode embeds it) and no standard — practitioner pattern is one shared script + per-agent config stubs (the sync-mcps.sh pattern).
- [Research-capture is unreliable as an instruction — needs a PreCompact hook](agentic-context-design/research-capture-is-unreliable-as-instruction-needs-precompact-hook.md) — 2026-06-26 — editing AGENTS.md doesn't reach running sessions; capture-at-the-end is the weakest moment; for multi-compaction sessions PreCompact is the right enforcement trigger (+ SessionEnd backstop). Why capture can't be trusted to the instruction alone.
- [Never tear down live agent-launch infrastructure mid-flight](agentic-context-design/never-tear-down-live-agent-launch-infrastructure-mid-flight.md) — 2026-06-26 — incident post-mortem: stopping the Headroom proxy under running sessions broke them + spawned a 40-proc CPU-pegging swarm; the master switch was tokensmash study mode:off, not a kill; ConnectionRefused was misleading (proxy-pin vs over-context deadlock); never print env (leaks secrets).
- [A gated refactoring loop is built as a general engineering-loop skill + a task-profile skill + a Workflow that enforces maker/checker structurally](agentic-context-design/refactoring-loop-as-skill-plus-workflow-composition.md) — 2026-06-26 — the 3-artifact A/B/Workflow decomposition + universal evidence contract; enforcement is structural (separate different-model checker + real oracle), not skill prose.
- [RTK/Headroom net-save ~20-25% on real tasks (not the inflated self-meters); net-negative on tiny tasks](agentic-context-design/token-saving-proxies-rtk-headroom-net-positive-on-real-tasks-but-self-metrics-inflated.md) — 2026-06-26 — own A/B data + TACO/Princeton/LLMLingua-2 + mroczek critique; "99.3%" measures stripped bytes not API cost; RTK block actively HARMS Codex (cat RTK.md probe → double-runs, +81%); caching covers $ so value proxies for context-window capacity.
- [Effective AGENTS.md structure + what belongs always-on](agentic-context-design/effective-agents-md-structure-and-what-belongs-always-on.md) — 2026-06-26 — Anthropic 200-line adherence cliff, Lost-in-the-Middle ordering, procedure→skill / always-do→hook / reference→file anti-patterns, scope separation, caching cuts $ not attention. The audit rubric.
- [A procedural Markdown spec is the right control-flow mechanism for high-stakes agent work when combined with fail-closed gates and cross-model verification, not self-rated confidence](agentic-context-design/procedural-md-spec-as-agent-loop-control-flow.md) — 2026-06-26 — 17 confirmed sources; adds June 2026 loop-engineering canon (Osmani O'Reilly June 22, Steipete tweet June 7, Cherny quote, /goal v2.1.139 May 11); maps 5-state gate to Osmani anatomy; resolves the hard problem: "is this ideal?" is the BAD non-checkable termination condition — the gate converts it to three checkable structural conditions (blind-derivations-converged + membership-against-spec + tests-green).
- [Agents retrieve knowledge via always-on instruction, not model judgment](agentic-context-design/agents-retrieve-knowledge-via-always-on-instruction-not-model-judgment.md) — 2026-06-25 — why a "check the corpus first" standing instruction is the only reliable defense against write-only rot; reliability ranking across 8 tools.
- [Grep beats embeddings for small structured corpora](agentic-context-design/grep-beats-embeddings-for-small-structured-corpora.md) — 2026-06-25 — empirical (SWE-Bench, Aider, Cody) basis for no-embeddings/no-vector-DB; when embeddings start to pay off.
- [Claude Code conditional context-injection + hook mechanics](agentic-context-design/claude-code-conditional-context-injection-hook-mechanics.md) — 2026-06-25 — hook events that can inject context, exact UserPromptSubmit schema, skills-vs-hooks, prompt-caching cost reality.
- [Claude PostToolUse wire schema for Bash (empirical)](agentic-context-design/claude-posttooluse-hook-wire-schema-bash.md) — 2026-06-25 — real fields (tool_response.stdout/stderr, NO exit_code); failure must be inferred from stderr; global hooks fire across all agents; .d.ts schema was wrong.

## feedback-systems
- [Event-gated feedback beats cadence for tool dogfooding](feedback-systems/event-gated-feedback-beats-cadence-for-tool-dogfooding.md) — 2026-06-25 — ESM + telemetry + UX consensus: trigger on friction/notable-success, rate-cap, thin staleness floor; generalizes across the tool roster.
- [Build simpler v1 feedback detector, not the generalized system](feedback-systems/dogfooding-feedback-build-simpler-v1-not-generalized-system.md) — 2026-06-25 — independent gpt-5.5 review + rebuttal: narrow the mechanism (roster.yaml, Claude-only candidate-detector, no Stop nudge, redaction/flock/kill-switch); 2-week trial decides sufficiency. The build spec.
- [feedback-block presence > owner/intent labels](feedback-systems/feedback-block-presence-encodes-report-vs-keep-drop-no-owner-intent-labels.md) — 2026-06-25 — Hickey review: report-vs-keep/drop collapses to "is there a feedback block"; cut owner/intent/stubs; nudge is destination-aware not intent-aware; fixed block-unbounded ledger lookup.

## knowledge-management
- [Separate facts from interpretation via sections, not files](knowledge-management/separate-facts-from-interpretation-via-sections-not-files.md) — 2026-06-25 — claim/evidence separation (Toulmin, nanopublications, zettelkasten); the design basis for this corpus's own format.
